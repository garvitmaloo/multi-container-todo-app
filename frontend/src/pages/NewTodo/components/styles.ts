import styled from "styled-components";
import { VerticalFlexWrapper } from "../../../styled";

export const InputsContainer = styled(VerticalFlexWrapper)`
  justify-content: flex-start;
  align-items: flex-start;
`;
